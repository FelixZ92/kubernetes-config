---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: prometheus-gatekeeper
spec:
  interval: 5m
  chart:
    spec:
      chart: gatekeeper
      version: '0.6.0'
      sourceRef:
        kind: HelmRepository
        name: felixz92
        namespace: kube-system
      interval: 10m
  values:
    config:
      encryptionKey:
        secretName: oidc-signing-secret
        secretKey: secret
      client:
        secretName: gitlab-client-credentials
        idKey: clientID
        secretKey: secret
      upstreamUrl: http://kube-prometheus-stack-prometheus:9090
      redirectionUrl: https://prometheus.${BASE_DOMAIN}
      discoveryUrl: https://gitlab.com
      skipTokenVerification: "false"

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-tls-certificate
spec:
  secretName: tls-prometheus-ingress
  issuerRef:
    name: ca-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - prometheus.${BASE_DOMAIN}
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: prometheus
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`prometheus.${BASE_DOMAIN}`)
      kind: Rule
      services:
        - name: prometheus-gatekeeper
          port: 3000
      middlewares:
        - name: redirect-https
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: prometheus-tls
  annotations:
    dashboard.zippelf.com/scrape: "true"
    dashboard.zippelf.com/logo: "https://avatars1.githubusercontent.com/u/3380462?s=200&v=4"
    dashboard.zippelf.com/display-name: "Prometheus"
    dashboard.zippelf.com/docs: "https://prometheus.io/docs/prometheus/latest/"
spec:
  entryPoints:
    - websecure
  tls:
    secretName: tls-prometheus-ingress
  routes:
    - match: Host(`prometheus.${BASE_DOMAIN}`)
      kind: Rule
      services:
        - name: prometheus-gatekeeper
          port: 3000
