resources:
  - postgres.yaml
  - ingress
  - init-keycloak.yaml
  - keycloak-release.yaml
  - monitor
  - k8s-keycloak-client.yaml
  - signing-secret.yaml
  - realms

configMapGenerator:
  - name: keycloak-values
    files:
      - values.yaml

generatorOptions:
  disableNameSuffixHash: true

namespace: keycloak
