resources:
  - ../../base

patches:
  - target:
      name: longhorn-gatekeeper
      kind: HelmRelease
    patch: |-
      - op: replace
        path: /spec/values/config/skipOpenidProviderTlsVerify
        value: true
