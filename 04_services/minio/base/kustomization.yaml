resources:
  - configmap.yaml
  - deployment.yaml
  - post-install-create-bucket-job.yaml
  - pvc.yaml
  - secrets.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/instance: minio
  app.kubernetes.io/name: minio

namespace: minio
