resources:
  - ../base
  - owncloud-admin-user.json

patchesStrategicMerge:
  - certificate-patch.yaml
  - ingressroute-patch-alertmanager.yaml

configMapGenerator:
  - name: owncloud-config
    behavior: merge
    literals:
      - OWNCLOUD_DOMAIN=owncloud.host.k3d.internal
