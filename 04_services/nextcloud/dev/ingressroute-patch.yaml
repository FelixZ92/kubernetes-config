---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud
spec:
  routes:
    - match: Host(`nextcloud.host.k3d.internal`)
      kind: Rule
      services:
        - name: nextcloud
          port: 8000
      middlewares:
        - name: redirect-https
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud-tls
spec:
  routes:
    - match: Host(`nextcloud.host.k3d.internal`)
      kind: Rule
      services:
        - name: nextcloud
          port: 8000
