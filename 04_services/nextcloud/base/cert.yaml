---
apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: owncloud-tls-certificate
spec:
  secretName: tls-owncloud-ingress
  issuerRef:
    name: ca-clusterissuer
    kind: ClusterIssuer
