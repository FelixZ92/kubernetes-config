resources:
  - deployment.yaml
  - ingress.yaml
  - service.yaml
  - config.yaml
  - pvc.yaml
  - hello.yaml
  - servicemonitor.yaml

configMapGenerator:
  - name: entrypoint
    files:
      - entrypoint-override.sh

generatorOptions:
  disableNameSuffixHash: true
