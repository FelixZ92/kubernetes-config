resources:
  - deployment.yaml
  - ingress.yaml
  - service.yaml
  - proxy-config-configmap.yaml
  - pvc.yaml

configMapGenerator:
  - name: entrypoint
    files:
      - entrypoint-override.sh

generatorOptions:
  disableNameSuffixHash: true
