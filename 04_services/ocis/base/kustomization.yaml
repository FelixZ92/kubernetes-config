resources:
  - deployment.yaml
  - ingress.yaml
  - service.yaml
  - config.yaml
  - pvc.yaml

configMapGenerator:
  - name: entrypoint
    files:
      - entrypoint-override.sh

generatorOptions:
  disableNameSuffixHash: true
