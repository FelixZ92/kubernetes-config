bases:
  - ../base

resources:
  - ../../prometheus-operator/dev/grafana-admin-user-sealed.json

patchesStrategicMerge:
  - sealed-secrets-repo-path.yaml
  - cert-manager-repo-path.yaml
  - prometheus-operator-env-patch.yaml
