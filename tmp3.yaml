apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  labels:
    kustomize.toolkit.fluxcd.io/name: cert-manager
  name: cert-manager
  namespace: cert-manager
spec:
  interval: 10m0s
  path: ./03_infrastructure/pki/cert-manager/base
  prune: true
  sourceRef:
    kind: GitRepository
    name: infrastructure
    namespace: flux-system
  validation: client
