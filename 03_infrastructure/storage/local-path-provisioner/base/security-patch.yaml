apiVersion: apps/v1
kind: Deployment
metadata:
  name: local-path-provisioner
  namespace: local-path-storage
spec:
  template:
    spec:
      securityContext:
        runAsNonRoot: false
      containers:
        - name: local-path-provisioner
          securityContext:
            runAsNonRoot: false