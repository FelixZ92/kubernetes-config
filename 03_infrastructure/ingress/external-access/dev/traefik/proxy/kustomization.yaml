resources:
  - ../../../common/louketo-proxy

namePrefix: traefik-

configMapGenerator:
  - name: louketo-proxy-config
    behavior: replace
    literals:
      - REDIRECTION_URL=https://traefik.192.168.0.13.xip.io
      - UPSTREAM_URL=http://forward-auth-bouncer/dashboard/
      - DISCOVERY_URL=https://keycloak.192.168.0.13.xip.io/auth/realms/k8s
      - SKIP_OPENID_PROVIDER_TLS_VERIFY=true
