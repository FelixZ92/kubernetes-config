---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: dashboard-gatekeeper
spec:
  values:
    config:
      redirectionUrl: http://localhost
      discoveryUrl: http://keycloak
      skipOpenidProviderTlsVerify: "true"
