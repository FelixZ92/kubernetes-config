---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: dashboard-gatekeeper
spec:
  values:
    config:
      redirectionUrl: https://dashboard.host.k3d.internal
      discoveryUrl: http://keycloak-http.keycloak.svc/auth/realms/k8s
      skipOpenidProviderTlsVerify: "true"
      skipTokenVerification: "false"
