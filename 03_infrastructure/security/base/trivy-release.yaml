---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: starboard
  namespace: starboard
spec:
  interval: 5m
  chart:
    spec:
      chart: trivy
      version: '0.4.4'
      sourceRef:
        kind: HelmRepository
        name: aquasecurity
        namespace: flux-system
      interval: 10m
  values:
    image:
      registry: docker.io
      repository: aquasec/trivy
      tag: 0.19.2
