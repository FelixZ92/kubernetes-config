resources:
  - ../../base/argocd

namespace: argocd

patchesStrategicMerge:
  - ingressroute-patch.yaml
  - certificate-patch.yaml

commonAnnotations:
  annotations:
    argocd.argoproj.io/sync-wave="4"
