resources:
  - ../../../oidc-proxy

namePrefix: traefik-

configMapGenerator:
  - name: gatekeeper-config
    behavior: replace
    literals:
      - REDIRECTION_URL=https://traefik.192.168.2.105.xip.io
      - UPSTREAM_URL=http://forward-auth-bouncer/dashboard/
      - DISCOVERY_URL=https://keycloak.192.168.2.105.xip.io/auth/realms/k8s
      - SKIP_OPENID_PROVIDER_TLS_VERIFY=true
