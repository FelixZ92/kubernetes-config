resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/name: gatekeeper

configMapGenerator:
  - name: gatekeeper-config
    literals:
      - REDIRECTION_URL=https://whoami.192.168.2.105.xip.io
      - UPSTREAM_URL=http://localhost:80
      - SKIP_OPENID_PROVIDER_TLS_VERIFY=false
