resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/name: louketo-proxy

configMapGenerator:
  - name: louketo-proxy-config
    literals:
      - REDIRECTION_URL=https://whoami.127.0.0.1.xip.io
      - UPSTREAM_URL=http://localhost:80
      - SKIP_OPENID_PROVIDER_TLS_VERIFY=false
