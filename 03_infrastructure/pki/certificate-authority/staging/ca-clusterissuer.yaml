apiVersion: cert-manager.io/v1alpha2
kind: ClusterIssuer
metadata:
  name: ca-clusterissuer
spec:
  acme:
    privateKeySecretRef:
      name: lets-encrypt-account-key
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: felix.zippel@mailbox.org
    solvers:
      - http01:
          ingress:
            class: traefik-internal
