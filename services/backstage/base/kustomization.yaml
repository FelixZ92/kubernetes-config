resources:
  - postgres.yaml
  - ingress.yaml
  - deployment.yaml
  - service.yaml

namespace: backstage
#
#secretGenerator:
#  - name: backstage-secrets
#    env: .env

generatorOptions:
  disableNameSuffixHash: true
