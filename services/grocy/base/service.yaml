apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: grocy
  name: backend
spec:
  ports:
    - name: backend
      port: 9000
      targetPort: backend
    - name: http
      port: 8080
      targetPort: http
    - name: https
      port: 8443
      targetPort: https
  selector:
    app.kubernetes.io/name: grocy
