resources:
  - deployment.yaml
  - ingress.yaml
  - service.yaml
  - config.yaml
  - hello.yaml
  - servicemonitor.yaml
  - secrets

configMapGenerator:
  - name: entrypoint
    files:
      - entrypoint-override.sh

generatorOptions:
  disableNameSuffixHash: true
