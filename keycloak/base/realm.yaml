apiVersion: keycloak.org/v1alpha1
kind: KeycloakRealm
metadata:
  name: basic
  labels:
    app: keycloak
spec:
  realm:
    id: "basic"
    realm: "basic"
    enabled: True
    displayName: "Basic Realm"
  instanceSelector:
    matchLabels:
      app: keycloak
