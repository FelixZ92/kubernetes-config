apiVersion: batch/v1
kind: Job
metadata:
  namespace: argocd
  name: check-keycloak-password
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  template:
    spec:
      containers:
        - name: check-keycloak-password
          image: registry.gitlab.com/felixz92/kubectl-git
          command:
            - "kubectl"
            - "-n"
            - "keycloak"
            - "get"
            - "secret"
            - "keycloak-admin-user"
      restartPolicy: Never
      serviceAccountName: secret-reader
  backoffLimit: 2
