namespace: traefik

resources:
  - certificate.yaml
  - ingressroute.yaml
  - service.yaml
  - forward-auth-container.yaml

configMapGenerator:
  - name: forward-auth-config
    literals:
      - PROVIDERS_OIDC_ISSUER_URL=https://keycloak.192.168.0.13.xip.io
      - DEFAULT_PROVIDER=oidc
      - LOGLEVEL=trace
