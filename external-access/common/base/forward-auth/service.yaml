apiVersion: v1
kind: Service
metadata:
  name: forward-auth
spec:
  selector:
    app.kubernetes.io/name: forward-auth
  ports:
    - name: forward-auth
      port: 4181
      protocol: "TCP"
      targetPort: "forward-auth"
  type: ClusterIP
