resources:
  - ../base
  - proxy

namespace: monitoring

patchesStrategicMerge:
  - certificate-patch-prometheus.yaml
  - ingressroute-patch-prometheus.yaml

commonAnnotations:
  annotations:
    argocd.argoproj.io/sync-wave="4"
