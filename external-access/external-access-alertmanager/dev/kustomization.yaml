resources:
  - ../base
  - proxy

namespace: monitoring

patchesStrategicMerge:
  - ingressroute-patch-alertmanager.yaml
  - certificate-patch-alertmanager.yaml

commonAnnotations:
  annotations:
    argocd.argoproj.io/sync-wave="4"
