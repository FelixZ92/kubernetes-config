resources:
  - ../../../../louketo-proxy

namePrefix: prometheus-

commonLabels:
  app.kubernetes.io/instance: louketo-proxy-prometheus

configMapGenerator:
  - name: louketo-proxy-config
    behavior: replace
    literals:
      - REDIRECTION_URL=https://prometheus.192.168.0.15.xip.io
      - UPSTREAM_URL=http://prometheus-operator-prometheus:9090
