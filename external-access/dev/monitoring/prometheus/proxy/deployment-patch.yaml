apiVersion: apps/v1
kind: Deployment
metadata:
  name: louketo-proxy
spec:
  template:
    spec:
      volumes:
        - name: ssl
          secret:
            secretName: dev-ca-secret
      containers:
        - name: gatekeeper
          volumeMounts:
            - mountPath: /etc/dev-ca
              name: ssl