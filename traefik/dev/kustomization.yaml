bases:
  - ../base

resources:
  - oidc-secret.json
  - signing-secret-sealed.json

patchesStrategicMerge:
  - ingressroute-patch-forward-auth.yaml
  - certificates-patch.yaml
  - ingressroute-patch-dashboard.yaml

configMapGenerator:
  - name: traefik-oidc-config-
    behavior: merge
    literals:
      - COOKIE_DOMAIN=192.168.0.15.xip.io
      - AUTH_HOST=auth.192.168.0.15.xip.io
