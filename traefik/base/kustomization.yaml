resources:
  - all.yaml
  - middlewares.yaml

namespace: traefik

configMapGenerator:
  - name: traefik-oidc-config-
    literals:
      - PROVIDERS_OIDC_ISSUER_URL=https://gitlab.com
      - DEFAULT_PROVIDER=oidc

patchesStrategicMerge:
  - forward-auth-container.yaml
