resources:
  - all.yaml
  - ../../external-access/base/traefik/middlewares.yaml
  - ../../external-access/base/traefik/certificates.yaml
  - ../../external-access/base/traefik/ingressroute-dashboard.yaml

namespace: traefik
#
#configMapGenerator:
#  - name: traefik-oidc-config-
#    literals:
#      - PROVIDERS_OIDC_ISSUER_URL=https://gitlab.com
#      - DEFAULT_PROVIDER=oidc
#      - LOGLEVEL=trace
