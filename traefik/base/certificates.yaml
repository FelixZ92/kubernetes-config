---
apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: traefik-dashboard-tls-certificate
spec:
  secretName: tls-traefik-dashboard-ingress
  issuerRef:
    name: ca-clusterissuer
    kind: ClusterIssuer
