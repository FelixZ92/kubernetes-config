---
apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: forward-auth-tls-certificate
  namespace: monitoring
spec:
  secretName: tls-forward-auth-ingress
  issuerRef:
    name: ca-clusterissuer
    kind: ClusterIssuer
